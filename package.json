{
  "name": "parcel-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -rf ./dist && parcel serve src/pages/*/*/*.html --no-cache ",
    "build": "parcel build src/pages/*/*/*.html --no-cache -d build  --no-content-hash --no-minify  --no-source-maps --detailed-report"
  },
  "author": "niuxinyu",
  "license": "MIT",
  "dependencies": {
    "@babel/runtime": "^7.9.2",
    "babel-polyfill": "^6.26.0",
    "swiper": "^5.3.6"
  },
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/plugin-transform-runtime": "^7.9.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "stylus": "^0.54.7"
  }
}
